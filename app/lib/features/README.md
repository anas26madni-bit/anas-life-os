# Feature modules

Product feature modules are introduced only in their approved sprints. Sprint 1
intentionally contains no product feature implementation.

Each future feature follows `data/`, `domain/`, and `presentation/` boundaries.
